import finnish from "./fi";

const getTranslations = () => {
  // const lang = "fi";
  //
  // if (lang === "fi") {
  return finnish;
  // }

  // throw new Error("Language not found");
};

export { getTranslations };
