export const bold = (input: string): string => `**${input}**`;
export const commentBlock = (input: string): string => `\`\`\`${input}\`\`\``;
export const italic = (input: string): string => `*${input}*`;
